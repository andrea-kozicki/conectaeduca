# REPO-01 — preparação semântica #89 ↔ #91

Data: 18/09/2026.

Esta branch é um **candidato de integração para revisão**, criado sem alterar
`main` e sem autorizar merge dos PRs existentes.

## Fontes

- #89: `c99ea01289c0cadbc0b62f92af2fc361cd3e27c2`
- #91: `b5eb1e2ee3bf0d14c4ad1f2899695b0af4506a78`

A branch parte do HEAD do #91 e aplica todas as alterações não conflitantes do
#89. Os dois arquivos compartilhados foram reconciliados semanticamente:

- `docs/release/HANDOFF-FINAL.md`;
- `scripts/release/verificar_handoff.sh`.

## Invariantes preservados do #89

- Storage Bacula emulado como último overlay;
- helper `preparar_storage_emulado.py` fail-closed;
- target persistido para redeploys;
- validações de capacidade, quiescência e rollback;
- regressões de infraestrutura;
- documentação operacional de Suricata/Wazuh e pfSense.

## Invariantes preservados do #91

- reconciliadores Wazuh API PKI, identidade `teste` e Dashboard ACL;
- validador operacional Wazuh;
- verificação de presença e sintaxe desses artefatos no bundle;
- Bacula FD package-based `bacula-client` + `/opt/bacula`, herdado da base #91.

## Limite

Esta branch não resolve #92→#94, não altera VMs e não substitui revisão do HEAD
final. Ela existe para permitir CI e revisão independente do ponto crítico
#89↔#91 antes de qualquer integração em `main`.
