# Histórico documental — ConectaEduca

Este diretório preserva artefatos que têm valor de rastreabilidade, mas **não
são fonte operacional atual**.

## Regra

Material em `docs/historico/`:

- não redefine o baseline atual;
- não deve ser usado como runbook das EP125/EP126;
- pode explicar decisões, hotfixes ou estados anteriores;
- deve apontar para a documentação vigente quando necessário.

## Conteúdo atual

### Laboratório local

- `lab-local/OPENBAO-RETOMADA-HOTFIX-V2.2.md` — hotfix histórico da retomada
  OpenBao/generate-root;
- `lab-local/RETOMADA-POS-REBOOT.md` — retomada do laboratório local anterior,
  com Mailpit/SMTP e launcher local.

### Snapshots

- `snapshots/estrutura-arquivos-repositorio-legado.txt` — árvore antiga do
  repositório, preservada apenas para mostrar a evolução estrutural.

Para a documentação vigente, comece em `../INDEX-DOCUMENTACAO.md`.
