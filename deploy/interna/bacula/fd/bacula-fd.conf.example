FileDaemon {
  Name = conectaeduca-interna-fd
  FDport = 9102
  WorkingDirectory = /var/lib/bacula
  Pid Directory = /run/bacula
  Maximum Concurrent Jobs = 2

  TLS Enable = yes
  TLS Require = yes
  TLS Verify Peer = yes
  TLS CA Certificate File = /etc/bacula/tls/ca.crt
  TLS Certificate = /etc/bacula/tls/interna-fd.crt
  TLS Key = /etc/bacula/tls/interna-fd.key
}

Director {
  Name = conectaeduca-dir
  Password = "__RUNTIME_SECRET_INTERNA_FD__"
}

Messages {
  Name = Standard
  director = conectaeduca-dir = all, !skipped, !restored
}
